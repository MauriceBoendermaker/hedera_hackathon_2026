GENERATE_SOURCEMAP=false
INLINE_RUNTIME_CHUNK=false

PORT=5000
REACT_APP_PROJECT_URL=http://localhost:5000
REACT_APP_ANALYTICS_URL=http://localhost:5000
REACT_APP_EXPLORER_URL=https://hashscan.io/testnet
REACT_APP_HEDERA_RPC_URL=https://testnet.hashio.io/api
REACT_APP_CONTRACT_ADDRESS=0xd8deBB79061f8eA999b6cF0f78D0e230367243cB
REACT_APP_CHAIN_ID=296

# Auth & privacy (persist across restarts)
TOKEN_SECRET=
IP_HASH_SECRET=

# Analytics tuning (all optional — defaults shown)
RETENTION_DAYS=90
# DEDUP_WINDOW_MS=600000
# TIMESTAMP_DRIFT_MS=300000
# HCS_TIMEOUT_MS=10000
# MAX_MAP_ENTRIES=100000
# BODY_SIZE_LIMIT=10kb

# Server-side contract config (for social preview route)
CONTRACT_ADDRESS=0xd8deBB79061f8eA999b6cF0f78D0e230367243cB
HEDERA_RPC_URL=https://testnet.hashio.io/api

# HCS Configuration (Phase 2)
# Server-side only — do NOT use REACT_APP_ prefix for secrets
OPERATOR_ID=0.0.7974872
OPERATOR_KEY=
HCS_TOPIC_ID=0.0.8191793
# Safe to expose — just a topic ID, not a secret
REACT_APP_HCS_TOPIC_ID=0.0.8191793
